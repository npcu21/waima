@extends('admin.layouts.app')

@section('title', 'Master Admin List')

@section('content')

<link href="{{ asset('css/style.css') }}" rel="stylesheet">

<div class="container-fluid p-0">

    <!-- Navbar (same as registration page) -->

<!-- Navbar -->
    @include('includes.navbar')

    <!-- Page Heading -->
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-12 col-md-12 p-4">

           
       <div class="d-flex justify-content-between align-items-center">
            <h4 class="mb-4">Administrator  List</h4>
            <a href="{{ route('masteradmin.register') }}" class="btn btn-primary mb-4">
                <i class="bi bi-plus-circle me-1"></i> Add Administrator
            </a>
        </div>

        <div class="form-section">       

          <table class="table table-bordered">
    <thead>
        <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Email</th>
            <th>Country</th>
            <th>Created At</th>
            <th>Action</th>
        </tr>
    </thead>

  <tbody>
    @foreach($admins as $admin)
        <tr>
            <td>{{ $admin->id }}</td>
            <td>{{ $admin->name }}</td>
            <td>{{ $admin->email }}</td>
            <td>
                {{ $admin->country_id ? ($admin->country ? $admin->country->name : '-') : 'Regional' }}
            </td>
            <td>{{ $admin->created_at ? $admin->created_at->format('d/m/Y') : '-' }}</td>
            <td class="d-flex align-items-center gap-2">
                <a href="{{ route('masteradmin.edit', $admin->id) }}" class="btn btn-primary btn-sm">
                    <i class="bi bi-pencil-square"></i> Edit
                </a>
                <form action="{{ route('masteradmin.delete', $admin->id) }}" method="POST" 
                      onsubmit="return confirm('Are you sure you want to delete this admin?');">
                    @csrf
                    @method('DELETE')
                    <button type="submit" class="btn btn-danger btn-sm">Delete</button>
                </form>
            </td>
        </tr>
    @endforeach
</tbody>

</table>

        </div>
            </div>
        </div>
    </div>

</div>

<style>
    body {
        background-color: #f8f9fa;
    }

    h2 {
        font-weight: 600;
    }

    .table {
        background-color: #fff;
    }
</style>

@endsection
